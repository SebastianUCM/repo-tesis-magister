\section*{Abstract}
\begin{spacing}{1.4}

The advancement of Artificial Intelligence (AI) in healthcare is limited by the lack of diverse, high-quality, and correctly labeled datasets, especially for rare cardiac conditions. This research presents the development of a parametric synthetic electrocardiogram (ECG) generator based on the superposition of asymmetric Gaussian functions. Unlike deep generative models (e.g., GANs) that lack physiological control, this parametric approach offers explicit interpretability and precise morphological representation of the P-QRS-T cycle.

By employing numerical optimization algorithms (L-BFGS-B) and using the MIT-BIH Arrhythmia Database as a seed, the model calibrates amplitudes, temporal positions and the baseline level to reproduce the morphology of real signals. Validation was carried out on 2,238 normal sinus beats from record 100 of that database, yielding a mean Pearson correlation of 0.9986 (standard deviation 0.0020) and a mean RMSE of 0.0496 (standard deviation 0.0162) in normalized signal units.

These figures correspond to the second iteration of the model. An initial twenty-parameter implementation reached a correlation of 0.9647 and an RMSE of 0.3985, and analysis of its residual error identified two specific defects: saturation of the upper bound on R-wave amplitude, and the absence of a baseline term. Correcting both reduced the RMSE by 87.6\% and drove the systematic residual bias to zero. Residual diagnosis, rather than the aggregate metric alone, proved to be the instrument that enabled this improvement.

These results confirm the viability of the parametric approach for reproducing P-QRS-T morphology in normal sinus rhythm. The integration of clinical rule engines (Rijnbeek/AHA) to simulate demographic variability and specific pathologies such as ST-segment elevation and Atrial Fibrillation is proposed as the immediate extension of this work, and falls outside the scope validated at this stage.

\vspace{0.5cm}

\noindent\textbf{Keywords:} Synthetic ECG, Gaussian Models, Numerical Optimization, Physiological Variability, Data Augmentation.

\end{spacing}
